<!DOCTYPE html>
<html>
  <head>
    <title>気象情報FP1</title>
    <meta charset="utf-8">
    <link href="flamepattern1.css" rel="stylesheet">
    <script>
        function set2fig(num) {
          // 桁数が1桁だったら先頭に0を加えて2桁に調整する
          var ret;
          if( num < 10 ) { ret = "0" + num; }
          else { ret = num; }
            return ret;
          }
          function showClock2() {
            var nowTime = new Date();
            var nowHour = set2fig( nowTime.getHours() );
            var nowMin  = set2fig( nowTime.getMinutes() );
            var nowSec  = set2fig( nowTime.getSeconds() );
            var msg = "現在時刻</br>" + nowHour + ":" + nowMin + ":" + nowSec ;
            document.getElementById("date").innerHTML = msg;
            }
            setInterval('showClock2()',1000);
    </script>
  </head>

  <body>
    <p id="title">愛子地区 気象情報</p>
    <p id="date"></p>

    <p id="E1credit1">気象データ観測値　<%= datatime %>時点</p>
    <div id="E1">
      <div id="temp"><%= temp %></div>
      <div id="humi"><%= humi %></div>
      <div id="wind_s"><%= wind_s %></div>
      <div id="wind_d"><%= wind_d %></div>
      <div id="wind_max_s"><%= wind_max_s %></div>
      <div id="press_l"><%= press_l %></div>
      <div id="rain_i"><%= rain_i %></div>
      <div id="rain_m"><%= rain_m %></div>
    </div>
    <p id="E1credit2">※風向・降水強度は参考値です</p>

    <p id="E2credit1">降水ナウキャスト(5分更新)</br>時点</p>
    <p id="E2credit2">出典：気象庁ホームページ(https://www.jma.go.jp/))</p>
  </body>
</html>
